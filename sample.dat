crear base_datos @MIKE13;
usar @MIKE13;
crear objeto conjunto(integer a, integer b);
crear tabla carnet(integer id);
insertar en tabla carnet (2);
crear tabla estudiante(Integer id no nulo unico autoincrementable, integer nombre llave_foranea carnet id no nulo,
integer p llave_primaria,  conjunto num no nulo);
##eliminar objeto conjunto;
##insertar en tabla   (a,b,c) valores (1,1,d);
##insertar en tabla estudiante (d,d,d) valores (1,1,1);
declarar d integer  = 1;
declarar obj conjunto;
obj.a = 125;
obj.b = 135;
mientras(d<10){
insertar en tabla estudiante (2,d,obj);
d = d+1;
}
insertar en tabla estudiante (2,d,obj);
##crear tabla uno45(integer dos no nulo, integer tres nulo llave_foranea estudiante id, conjunto uno);
##eliminar tabla estudiante;
##eliminar tabla carnet;
2<2 && 1<1;

